rootProject.name = 'app'
toolchainManagement {
    // Adoptium을 기본적으로 사용하는 설정 예시
    standardToolchains {
        // Gradle이 자동으로 JDK를 다운로드할 저장소를 설정
        // 이를 통해 로컬 환경에 Java 21이 없어도 빌드가 가능해짐
        vendor.set(JvmVendor.ADOPTIUM)
    }
}